module.exports = {
    startApi: {                      // Target
        options: {                      // Options
            stderr: true,
            stdout: true
        },
        command: '../miner-api/startup.sh'
    }
};